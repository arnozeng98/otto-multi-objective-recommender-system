extends: base.yaml

project:
  artifacts_dir: artifacts/single_gpu

candidates:
  workers: 4
  chunk_sessions: 500

covisitation:
  reduction_workers: 2

ranking:
  device: cuda

neural:
  embedding_dim: 64
  batch_size: 96
  gradient_accumulation_steps: 6