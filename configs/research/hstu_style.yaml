extends: ../base.yaml

project:
  artifacts_dir: artifacts/research/hstu_style

neural:
  embedding_dim: 48
  max_sequence_length: 64
  batch_size: 48
  gradient_accumulation_steps: 12
  precision: bf16