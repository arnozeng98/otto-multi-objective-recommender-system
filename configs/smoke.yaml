extends: base.yaml

project:
  artifacts_dir: artifacts/smoke

covisitation:
  partitions: 2
  max_neighbors: 20

ranking:
  device: cpu
  rounds: 20

neural:
  embedding_dim: 16
  max_sequence_length: 12
  batch_size: 8